# Add this to E:/agent_learning_route/.env (the route root) — never commit.
# GEMINI_API_KEY and GEMINI_MODEL are already there from earlier exercises.

ZAI_API_KEY=your-zai-key-here

# Used by providers.py:
#   priority 1: glm-4.5-flash      @ https://api.z.ai/api/paas/v4
#   priority 2: gemini-flash-latest @ googleapis (existing key)
